fn main() {
    let input = "d4input.txt":readFile():split("\n");
    let nights = [];
    let current = [];
    for _, line in input {
        if line:len() == 0 { continue; }
        if line[20] == 'G' {
            id = (line[27] - '0') * 10 + line[28] - '0';
            print id;
            current = [];
            nights:push(current);
        } else {

        }
    }
}
